import React from "react"
import ReactDOM from "react-dom"
import App from "./App"

ReactDOM.render(<App/>,document.getElementById("root"))

/* React DJ

Create App.js which holds the squares componenet and button box componenet.  
Within squares componenet are the 4 div boxes to be affected by the buttons.
Create seperate Button componenets to interact with the boxes 
*/
